<div class="partner-item sequenced-bottom">
    <picture>
        <source type="image/webp" srcset="{{ $webpSrc }}">
        <source type="image/png" srcset="{{ $normalSrc }}">
        <img data-src="{{ $normalSrc }}" alt="{{ $name }}" class="lazy" width="300" height="150">
    </picture>
</div>
